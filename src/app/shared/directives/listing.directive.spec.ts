import { ListingDirective } from './listing.directive';

describe('ListingDirective', () => {
  it('should create an instance', () => {
    const directive = new ListingDirective();
    expect(directive).toBeTruthy();
  });
});
