function showName(name: number): string {
    return 'some name ' + name;
    // console.log('some name ' + name);
}
let nameVal = 'SDLC Training';
let result = showName(nameVal);
console.log(result.toLowerCase());
